[project]
name = "cpu-freq-perf"
version = "0.1.0"
description = "Plot CPU frequency, thermal, and utilization charts from CSV logs."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.8",
    "pandas>=2.2",
]

[tool.uv]
package = false
